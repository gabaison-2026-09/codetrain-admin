# codetrain-admin のローカル設定
#
# admin は compose 管理外（WSL ホストで next dev を動かす）ため、
# 環境変数は codetrain-devenv/.env ではなく **このファイル**から読む。
# 使うときは cp .env.local.example .env.local
#
# ブラウザ（Windows 側）から見た API の URL。compose のサービス名 "api" ではない。
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
